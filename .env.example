# MODE options:
#   mock    — all responses are local, no network calls (safe offline demo)
#   sandbox — calls api.shipstation.com/v2 with test key
#   live    — calls api.shipstation.com/v2 with production key

SHIPSTATION_API_KEY=your_api_key_here
MODE=sandbox
PORT=3001
